
#include <iostream>
#include "Node1.h"
//#include "BinTree.h"


using namespace std;

int main()
{
    cout << "Hello World";
    //BinTree* tree = new BinTree();
    
    Node* root = new Node(NULL, (char*)("Žije to na púši?"));
   // tree->createChild(root, LEFT);

    return 0;
}